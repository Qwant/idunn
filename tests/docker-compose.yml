version: '3'

services:
  mimir_es:
    image: elasticsearch:2-alpine
    ports:
    - "9200"

  wiki_es:
    image: elasticsearch:2-alpine
    ports:
    - "9200"

  wiki_redis:
    image: redis:3.2-alpine
    ports:
    - "6379"
