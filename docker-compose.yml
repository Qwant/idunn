version: '3'

services:
  idunn:
    image: qwantresearch/idunn
#    deploy:
#      placement:
#        constraints:
#          - node.labels.type == node1
    environment:
      - IDUNN_MIMIR_ES=http://es:9200
    ports:
      - "5000:5000"
    expose:
      - 5000
