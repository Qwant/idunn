version: '3'

services:
  idunn:
    image: qwantresearch/idunn
    deploy:
      labels:
        - "traefik.port=5000"
        - "traefik.frontend.rule=Host:idunn.maps.qwant.loc"
    environment:
      - IDUNN_MIMIR_ES=http://es:9200
    expose:
      - 5000
